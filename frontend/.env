# Foxglove Studio (browser) — optional
# After `npm run dev`, Vite reads this file. Restart dev server when you change it.

# Full WebSocket URL to foxglove_bridge on the robot (must match where ROS runs).
VITE_FOXGLOVE_WS_URL=ws://192.168.68.107:8765

# Studio web app (default is fine).
# VITE_FOXGLOVE_APP_URL=https://app.foxglove.dev
